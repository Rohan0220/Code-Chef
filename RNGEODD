#include <stdio.h>

int main()
{
    int L,R;
   scanf ("%d",&L);
    scanf("%d",&R);
    for(int i = L; i<=R; i++)
    {
      if(i%2!=0)
     {
       printf("%d ",i);
     }
        
   }
   return 0;
}
